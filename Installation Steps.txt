Angular Installing commands:

> install node js
> node --version
> npm --version

> npm install -g @angular/cli@13.0.4

Create first app in Angulardemo folder:

> ng new myapp --no-strict
> cd myapp 
> For Running app: ng serve --open &


Intsalling Bootstrap:

> npm install --save bootstrap@4
> After installed:
> got vS-> anuglar.json-> style ->"node_modules/bootstrap/dist/css/bootstrap.min.css",


For typescript demo:
create directory typescriptdemos
cd typescriptdemos
npm install typescript --save-dev
npx tsc
npx tsc --init // this will create tsconfig.json
modify tsconfig.json and add the following lines
{
"include": ["src"],
"compilerOptions": {
"outDir": "./build"
}
}
create 2 subfolders - src and build
create demo1.ts file in src folder with typescript code
compile them with - npx tsc
run the compiled javascript - node build/demo1.js
>npm install typescript --save -dev
>npx tsc --init

For running typescrit file:
>npx tsc
>node build/demo1.js